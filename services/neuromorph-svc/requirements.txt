services/neuromorph-svc/requirements.txtThis file lists the Python packages required by the neuromorph-svc.-- Core API & Server Dependencies --A modern, fast (high-performance) web framework for building APIs.fastapi==0.111.0The ASGI server that runs the FastAPI application.uvicorn[standard]==0.30.1Pydantic is used by FastAPI for data validation and settings management.pydantic==2.7.4-- Optional & Neuromorphic SDK Dependencies --In a real-world scenario with actual hardware, you would uncomment andinstall the specific SDK provided by the vendor.For simulating spiking neural networks:brian2==2.5.4For Intel's Loihi hardware:lava-dl==0.4.0
