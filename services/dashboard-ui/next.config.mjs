// services/dashboard-ui/next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuration options for the Next.js application.
  // For this project, we can start with the default settings.
  //
  // Example options you might add later:
  //
  // reactStrictMode: true, // Helps identify potential problems in the app.
  //
  // async rewrites() {
  //   return [
  //     {
  //       source: '/api/:path*',
  //       destination: 'http://api.qsn.local/:path*', // Proxy API requests
  //     },
  //   ]
  // },
};

export default nextConfig;
